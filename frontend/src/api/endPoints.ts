const EndPoints = {
    getProducts : '/categories?populate[products]][fields][0]=name&populate[products][fields][1]=description&populate[products][fields][2]=price&populate[products][populate][image][fields][0]=url',
}

export default EndPoints;